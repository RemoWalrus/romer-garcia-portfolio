# Reverb Main Page Content Placement

## Changes
- Hide the visible “The Universe” section while keeping the rest of the page unchanged.
- Move each character’s existing blurb into its character panel.
- Keep all blurbs visible in the desktop horizontal layout.
- On mobile, show a blurb only inside the currently expanded character panel; collapsed rows remain name-only.
- Remove the separate caption strip to avoid duplicate text.

## Validation
- Check the Reverb page at desktop and mobile widths.
- Confirm captions remain readable without covering names, roles, or profile buttons.
- Run the project’s existing validation command.
